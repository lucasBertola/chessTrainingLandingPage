{
  "name": "frontend-app",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "deploy": "ng build --base-href=\"./\" && gh-pages -d dist/frontend-app",
    "deploy-simple": "gh-pages -d dist/frontend-app"
  }
} 